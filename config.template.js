const config = {
  // Token del bot telegram
  token: "",

  // Password per accedere all API (una stringa casuale)
  password: "",

  // channel ID che identifica il canale telegram dentro il quale il bot deve mandare il bollettino
  channelID: 00000 ,

  // Dati di connessione al database mongodb
  userdb: '',
  passworddb: "",
  mongourl: ""
};

module.exports = config;
